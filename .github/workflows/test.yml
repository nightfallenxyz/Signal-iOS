name: Check Environment
on:
  workflow_dispatch:
jobs:
  check:
    runs-on: macos-latest
    steps:
      - name: Check Xcode versions
        run: |
          ls /Applications/ | grep Xcode
          swift --version
          xcodebuild -version
